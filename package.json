{
  "name": "web-monitoring-app",
  "version": "0.1.0",
  "bin": {
    "web-monitoring-app": "bin/web-monitoring-app.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "start": "nodemon --watch lambda/ --exec \"cdk deploy\""
  },
  "devDependencies": {
    "aws-cdk": "2.146.0",
    "jest": "^29.7.0",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.602.0",
    "@aws-sdk/lib-dynamodb": "^3.602.0",
    "aws-cdk-lib": "2.146.0",
    "constructs": "^10.0.0"
  }
}
